# Cloudflare Pages (plataforma activa desde 2026-09-08) ya sirve automática
# y nativamente /dashboard, /login, /gestion-interna y /asistencia como URLs
# limpias de sus .html correspondientes (sin necesidad de reglas explícitas)
# — de hecho, agregar una regla 200 explícita para esos mismos paths choca
# con ese comportamiento nativo y produce un bucle de redirección 308 sobre
# sí mismo (encontrado y corregido 2026-09-14, ver minuta). Este archivo
# queda vacío de reglas a propósito.
